function result = addTwoNumbers(a, b)
%ADDTWONUMBERS Add two numeric values.
%   result = ADDTWONUMBERS(a, b) returns the sum of a and b.

arguments
    a (1,1) double
    b (1,1) double
end

result = a + b;
end
