function result = divideTwoNumbers(a, b)
%DIVIDETWONUMBERS Divide two numeric values.
%   result = DIVIDETWONUMBERS(a, b) returns the quotient of a divided by b.

arguments
    a (1,1) double
    b (1,1) double
end

result = a / b;
end
