function result = multiplyTwoNumbers(a, b)
%MULTIPLYTWONUMBERS Multiply two numeric values.
%   result = MULTIPLYTWONUMBERS(a, b) returns the product of a and b.

arguments
    a (1,1) double
    b (1,1) double
end

result = a * b;
end
